myHost=localhost
server.port=8887
#GitHub oauth 认证客户端
github.client.id=xxx
github.client.secret=xxx
# GitHub oauth 认证回调地址
github.redirect.url=http://localhost:8887/callback
spring.datasource.url=jdbc:mysql://localhost/majiang?useUnicode=true&characterEncoding=UTF-8
spring.datasource.username=root
spring.datasource.password=123456
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# Mybatis Generator 配置
mybatis.configuration.map-underscore-to-camel-case=true
mybatis.type-aliases-package=life.majiang.community.model
mybatis.mapper-locations=classpath:mapper/*.xml
# 日志设置
logging.file.name=logs/majiang.log
logging.level.root=info
logging.file.max-history=15
logging.file.max-size=10MB
# 防止数据库连接超时
spring.datasource.hikari.max-lifetime = 600
spring.datasource.hikari.idle-timeout = 600
# 邮件服务
spring.mail.host = smtp.qq.com
spring.mail.username = xxx@qq.com
# 并非邮箱账号密码，而是第三方登录授权码
spring.mail.password = efzogkkefvtzebge
spring.mail.properties.mail.smtp.ssl.enable=true
spring.mail.default-encoding=UTF-8